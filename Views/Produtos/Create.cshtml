@model GestaoPedidos.Models.Produto

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Space+Grotesk&display=swap"
        rel="stylesheet">
    <style>
        .bg-gradient-red {
            background: linear-gradient(135deg, #501211, #8b1c1d);
            color: white;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
        }

        .btn-primary {
            background: linear-gradient(135deg, #1b6ec2, #1861ac);
            border: none;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #1861ac, #1b6ec2);
        }

        h1 {
            font-family: 'Space Grotesk', sans-serif;
            font-weight: bold;
            color: #501211;
            text-align: center;
            margin-bottom: 2rem;
        }

        .form-group label {
            color: #501211;
            font-weight: bold;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .card {
            border: 1px solid #501211;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 2rem;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="card">
            <h1>Criar Produto</h1>

            <form asp-action="Create" method="post">
                <div class="form-group">
                    <label for="Nome">Nome</label>
                    <input asp-for="Nome" required class="form-control" />
                </div>

                <div class="form-group">
                    <label for="Preco">Preço</label>
                    <input asp-for="Preco" required class="form-control" type="number" step="0.01" />
                </div>

                <button type="submit" class="btn btn-primary">Criar Produto</button>
            </form>
        </div>
    </div>
</body>
